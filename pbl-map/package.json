{
  "name": "pbl-map",
  "version": "2.0.0",
  "description": "K12 学期覆盖包 — 多 PBL 全覆盖 + 个人台账",
  "private": true,
  "scripts": {
    "build:index": "node scripts/build-cn-k12-index.mjs",
    "build:k12": "node scripts/build-k12-pbl-curriculum.mjs",
    "build:decompose": "node scripts/build-all-decompose-demos.mjs",
    "build": "npm run build:index && npm run build:k12 && npm run build:decompose",
    "serve": "npx --yes serve . -p 3456"
  }
}
