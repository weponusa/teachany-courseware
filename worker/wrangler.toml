name = "llm-proxy"
main = "src/index.js"
compatibility_date = "2024-12-01"

[vars]
# 环境变量通过 wrangler secret 设置，不写在代码中
# PARATERA_KEY, OPENROUTER_KEY
#
# Cloudflare Pages（teachany-courseware）加密环境变量：OPENROUTER_KEY（PBL 主）、SILICONFLOW_KEY（兜底）、PARATERA_KEY（兜底）

# 路由：llm-proxy.teachany.workers.dev
